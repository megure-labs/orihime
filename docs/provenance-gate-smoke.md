# Provenance gate smoke test

This temporary pull request intentionally omits its required Kaname provenance record so the repository can verify that the merge gate fails closed.

The follow-up commit exercises the pull request `synchronize` event explicitly.
